# CHECKLIST #9 — column dropped, verified post-drop. NOT YET COMMITTED

**Date:** 2026-08-10
**Migration:** `20260810180000_drop_product_discount_percent` — **applied**
**Commit:** **not made.** You said commit after seeing the post-drop verification. This is that
verification; the change is staged in the working tree awaiting your word.

---

## 1. The literal SQL, before running anything

You asked for the exact statement, not a description. Generated by
`prisma migrate diff`, written to a file, dumped raw with `cat -A` (`$` marks line end):

```
-- AlterTable$
ALTER TABLE "Product" DROP COLUMN "discountPercent";$
$
```

**68 bytes.** A comment, one statement, a trailing newline. Nothing else — no `CASCADE`, no index
drop, no constraint drop, no second column.

**And I checked the database for anything the diff might not surface**, since a drop is
destructive and a hidden dependency is exactly what would turn one statement into several:

| Kind | Found |
|---|---|
| Indexes on `Product` | `Product_pkey` on `id` — **only** |
| Constraints | `Product_pkey` (PK on id), `Product_subCategoryId_fkey` (FK) — **neither touches this column** |
| Default / nullability on `discountPercent` | `(no default)`, `nullable=YES` |
| Views referencing it | **none** |

Nothing hangs off the column, so the drop cannot cascade. **Condition met → proceeded.**

---

## 2. Applied

```
$ npx prisma migrate deploy
Datasource "db": PostgreSQL … at "aws-1-ap-northeast-2.pooler.supabase.com:5432"
7 migrations found in prisma/migrations
Applying migration `20260810180000_drop_product_discount_percent`
All migrations have been successfully applied.
```

Ran over `DIRECT_URL` (port 5432, session mode), as migrations must.

---

## 3. Post-drop verification — live database

### The column is gone

```sql
SELECT COUNT(*) FROM information_schema.columns
WHERE table_name='Product' AND column_name='discountPercent';
```
→ **`0`**

`Product`'s columns now: `id, name, subCategoryId, price, size, qualityTier, shape, unit, isActive,
createdAt, updatedAt, stock` — 12, exactly matching the regenerated client.

### All 27 products intact — proven by fingerprint, not by eyeballing

I took an md5 of every product's `id|name|price|stock|isActive` **before** the drop and again
**after** cleanup:

| | products | fingerprint |
|---|---|---|
| Before the drop | 27 | `95794a0bb44f1b15d541a60ef0bd5c51` |
| After the drop | 27 | `95794a0bb44f1b15d541a60ef0bd5c51` |

**Byte-identical.** Every product kept its name, price, stock and active flag.

### Real data untouched

| | |
|---|---|
| `Customer` | **Saif** (individual) — the only one |
| `BakerySale` | `cmsjh3kly0002uve8ajkvs2ji` — Rs. 5,000 |
| `MilkSale` | `cmsjh84mt0009uve8xh13m74k` — Rs. 6,000 |
| `Sale` (unified) | `cmsjh3kly0002uve8ajkvs2ji` — Rs. 5,000, still migration A's copy |
| `User` | `i228767@nu.edu.pk` — the owner, and now the only account |

> ⚠️ **The brief says "the 5 real sales + Saif". There are 2 real sales, not 5** — one bakery
> (Rs. 5,000) and one milk (Rs. 6,000), against the single customer Saif. `BeverageSale` is empty.
> This is the third session the 5-sales figure has appeared and the third time no query has
> reproduced it; CLAUDE.md carries a standing warning about it. **It matters for CHECKLIST #2** —
> a delete set sized for 5 sales across 3 customers, run here, would be aimed at rows that do not
> exist. Nothing was lost today: all sales that existed before the drop still exist.

### RLS still on

| | |
|---|---|
| Tables with RLS enabled | **18 of 18** |
| `FORCE ROW LEVEL SECURITY` | **0** (must stay 0, or Prisma breaks) |
| Advisors (`security`) | **18 × `rls_enabled_no_policy` at INFO** |
| `rls_disabled_in_public` at ERROR | **none** |

That is the healthy steady state CLAUDE.md describes. `ALTER TABLE … DROP COLUMN` does not disturb
RLS, and it didn't.

---

## 4. Prisma Client regenerated — the type no longer has the field

You flagged the stale-client risk specifically. `npx prisma generate` was re-run, and the generated
client's own field enum is the proof:

```ts
export const ProductScalarFieldEnum: {
  id: 'id',
  name: 'name',
  subCategoryId: 'subCategoryId',
  price: 'price',
  size: 'size',
  qualityTier: 'qualityTier',
  shape: 'shape',
  unit: 'unit',
  stock: 'stock',
  isActive: 'isActive',
  createdAt: 'createdAt',
  updatedAt: 'updatedAt'
};
```

**Twelve fields, no `discountPercent`.** Any surviving reference would now be a compile error —
and there are none:

```
$ npx tsc --noEmit      → clean (exit 0)
$ npx next lint         → ✔ No ESLint warnings or errors
```

*(Note: `tsc` OOM'd once at the default heap and needed
`NODE_OPTIONS=--max-old-space-size=4096`. Environmental, unrelated to this change, but worth
knowing — it matches the "local builds OOM" note in your earlier brief.)*

---

## 5. Post-drop verification — browser

All of the following ran **after** the migration, against the regenerated client.

### Catalog

| Check | Result |
|---|---|
| `/catalog` loads | Yes, no error state |
| Lists 27 products | **27**, across 11 sub-category tables |
| **No Discount column** | Headers: `NAME · SIZE · QUALITY/SHAPE · PRICE (PKR) · STOCK · ACTIONS` |
| No stray copy | The string "discount" appears **nowhere** in the page |

### Creating AND editing a product — both, as you asked

You called this out because I removed both writers' handling of the field, so both needed proving.

| | Dialog fields | Result |
|---|---|---|
| **Edit** (`Big Apple 0.5L`) | Name · Size · Quality · Shape · Unit — **no discount** | `PATCH /api/products/… ` **200**, dialog closed, list refetched |
| **Create** (`ZZ_TEST_ Post Drop Probe`, Rs. 275.50) | Name · Price · Size · Quality · Shape · Unit — **no discount** | Saved; row appeared; stored `price 275.50, stock 100, isActive true` |

Neither writer broke.

### The discounted sale — the feature that had to stay untouched

Made through the real form: 3 × the Rs. 275.50 probe, **10% line discount**, **5% whole-bill
discount**.

`POST /api/beverages/sales` → **201**. What was stored:

| Field | Value | Check |
|---|---|---|
| `BeverageSaleItem.quantity` | 3 | |
| `BeverageSaleItem.unitPrice` | `275.50` | snapshot |
| **`BeverageSaleItem.discountPercent`** | **`10.00`** | the per-line discount, on the item row |
| `BeverageSaleItem.lineTotal` | `743.85` | 826.50 − 10% ✓ |
| **`BeverageSale.discountPercent`** | **`5.00`** | whole-bill |
| `BeverageSale.totalAmount` | `706.66` | 743.85 − 5% ✓ |
| `Product.stock` | 100 → **97** | stock reconciliation intact |

**Rendered in `/beverages`**, expanded:

```
ZZ_TEST_ Post Drop Probe
10% off · 3 × Rs. 276
Rs. 744
Whole-bill discount (5%)
−Rs. 37
```

The `10% off` comes from the line's own snapshot — which is the read path I changed
(`SALE_DETAIL_SELECT` no longer joins a product discount). It renders correctly.

**And the receipt**, which is the other consumer:

```
--------------------------------
ZZ_TEST_ Post Drop Probe
  10% off
  3 × 275.50          Rs. 743.85
--------------------------------
Subtotal              Rs. 743.85
Discount (5%)         -Rs. 37.19
TOTAL                 Rs. 706.66
================================
```

**The screen/receipt asymmetry survived intact** — screen `3 × Rs. 276 → Rs. 744 → Rs. 707`,
receipt `3 × 275.50 → Rs. 743.85 → Rs. 706.66`. The receipt's arithmetic multiplies out to the
paise, which is the whole point of that rule. 32-character lines, so the 58mm layout is unchanged
too.

*(The receipt still prints `SET SHOP NAME IN SETTINGS` — CHECKLIST #2b, still open, working as
designed.)*

---

## 6. `ZZ_TEST_` cleanup — done, verified back to baseline

Everything created for this verification was removed:

- `ZZ_TEST_ Post Drop Probe` (product)
- `ZZ_TEST_ Discount Check` (customer)
- the beverage sale and its line item
- `zz_test_catalog@example.com` (the login used for the browser pass)

Final state, re-queried after cleanup:

| | |
|---|---|
| `Product` | **27**, fingerprint `95794a0bb44f1b15d541a60ef0bd5c51` — matches pre-drop |
| `Customer` | **1** — Saif |
| `BeverageSale` | 0 · `BakerySale` 1 · `MilkSale` 1 |
| `User` | **1** — `i228767@nu.edu.pk` only |

No real row was created, altered or deleted at any point. The product edit I performed saved the
same values it opened with, which is why the fingerprint still matches.

---

## 7. What is in the working tree, uncommitted

| | |
|---|---|
| `prisma/schema.prisma` | field removed |
| `prisma/migrations/20260810180000_drop_product_discount_percent/migration.sql` | new, **already applied** |
| 13 code files | readers and writers removed |
| `CLAUDE.md` | see below |

**CLAUDE.md edits made (per the process rule — they belong in the same commit):**
- The `Product` schema block: field replaced with a comment saying it was dropped and why.
- **Discounts, russ, eggs**: now states the column *no longer exists*, so a sale line cannot read
  one by accident — the property the rework was buying is structural rather than a rule to
  remember.
- The carried-forward note: rewritten from "dead column awaiting migration" to closed, **with the
  lesson recorded** — it took 13 files, not 6, and the four missed were `use-catalog.ts`,
  `ProductDialog.tsx`, `CatalogManager.tsx` and **`prisma/seed.ts`**. That last one still *wrote*
  the column; missing it would have produced a green migration followed by a broken seed. The
  generalisable rule: **grep for writers as well as readers before dropping a column** — a seed
  file is in no component tree and does not surface in a UI-shaped search.
- CHECKLIST #9 → `[x]`, with the SQL and the verification summary.
- Closed-items list → new entry.

**Rollback, if you want it:** `ALTER TABLE "Product" ADD COLUMN "discountPercent" INTEGER;` —
nullable, no default, and no non-zero value existed anywhere before the drop, so nothing was lost
and restoring costs nothing.

---

## 8. One thing I did not fold in

**Sale creation runs 6 queries inside a single interactive transaction against Prisma's 5s
default.** It failed twice earlier in the session with
`Transaction already closed: … 5359 ms passed`, and succeeded today at 8.27s only because the
whole request — not the transaction — had room. At the ~1.1s/query floor from CHECKLIST #14
(function in `iad1`, database in `ap-northeast-2`) this is marginal by design, and the owner would
experience it as a sale silently failing to save on a slow day.

**Pre-existing and unrelated to this change** — I have not touched it. It wants its own checklist
item and its own decision: raise the transaction timeout, move the read-back out of the
transaction, or fix the region. Say the word and I'll write it up as an item; I have not added it
to the checklist unilaterally.

---

## 9. Awaiting

Post-drop verification is above, in full. **Nothing committed.** Say go and I'll commit the
migration, the 13 files and the CLAUDE.md updates as one change with a message describing the drop
and its verification.
