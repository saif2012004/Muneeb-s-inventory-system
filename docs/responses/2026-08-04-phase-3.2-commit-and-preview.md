# Response — CLAUDE.md note, commit, ignore check, preview deploy

**Date:** 4 Aug 2026
**Task:** add the durable `networkMode` note to CLAUDE.md, commit 3.2, confirm env/vercel
files are ignored, push, run a preview deploy, then stop.
**Outcome:** all done. **Stopped** — no delete-guard re-test, no Phase 4.

---

## 1. Flagged first: 3.2 was already committed

**Phase 3.2 was committed and pushed at the end of the previous turn** as `72fc56d` —
`feat: phase 3.2 - beverages sale UI (verified)`.

So the message requested this time — `feat: phase 3.2 - beverages sale UI (verified,
offline-safe)` — does not match what is on the remote. I did **not** rewrite it: amending a
pushed commit requires a force-push, which is destructive and outward-facing, so it is your
call.

**Two options, your pick:**

| Option | Effect |
|---|---|
| **Leave as is** (current state) | `72fc56d` + `889890a` together tell the story accurately |
| **Amend + force-push** | Folds the CLAUDE.md note into one commit named exactly `feat: phase 3.2 - beverages sale UI (verified, offline-safe)`. Solo repo, pushed minutes ago, so risk is low — but needs your go-ahead |

---

## 2. CLAUDE.md note added — commit `889890a`

Placed under **API Route Conventions → "Client data fetching (GLOBAL — applies to every
module, not just beverages)"**, carrying your wording plus the reasoning, and ending with the
explicit instruction: **"Do NOT revert to the default `networkMode`."**

It includes a two-row table so a future session can see why *both* halves are load-bearing:

| Failure | Without the setting | With it |
|---|---|---|
| No connection (`navigator.onLine` false) | Query's default `"online"` mode **pauses** the mutation — `mutationFn` never called, so no request, no error, no toast; a submit button disabled while pending stays disabled **forever** | Request runs, fails immediately, button re-enables with "Can't reach the server." |
| Connected but stalled | `fetch` never settles, so api-client's catch never runs — same permanent "Saving…" | Aborts at 15s into the existing catch |

### Also updated in the same commit

- **Phase 3 marked ✅ Done** in the phases table.
- **Carried-forward notes for Phase 4 (Bakery)** added:
  - `reconcileSaleLines()` in `lib/sales.ts` is THE price-snapshot implementation — reuse it,
    do not re-derive the predicate. Same for `SALE_DETAIL_SELECT` and `loadSaleProducts`.
  - The global TanStack Query settings are not optional.
  - The delete-guard 409 / soft-delete re-test is **still outstanding**, and now needs sales
    re-created first because the test DB was reset.
  - **Verify UI in a real browser, not on a build** — 3.2 type-checked, linted and built green
    while still carrying three real bugs, one of which trapped the owner with no way out.

---

## 3. Secrets / ignore check — clean

| Pattern | Status |
|---|---|
| `.env`, `.env.local`, `.env.production` | ignored via `.env*` (`.gitignore:32`) |
| `.vercel`, `.vercel/project.json` | ignored (`.gitignore:36`) |
| Tracked env files | only `.env.example` |

`.env.example` was read in full: **placeholders only** (`<project-ref>`, `<password>`,
`NEXTAUTH_SECRET=""`). No real credentials tracked anywhere.

---

## 4. Preview deploy — GREEN

| | |
|---|---|
| Result | **READY**, `✓ Compiled successfully` |
| `target` | **preview** — confirmed via `vercel inspect`; production untouched |
| URL | `https://muneeb-inventory-system-9fajuwrix.vercel.app` |
| Deployment | `dpl_2o9YVfsT9bsTK4NVi1kcjaQFt4S6` |
| `/beverages` | 4.44 kB (221 kB first load) |
| `/beverages/new-sale` | 31.8 kB (286 kB) |
| Middleware | **78.2 kB** — unchanged, Edge split still holds |
| Build warnings | only the known `package.json#prisma` v7 notice — ignore, pinned to v6 |

**The four fixes cost ~1 kB total** (221/286 kB vs 220/285 kB before) — `networkMode` and the
timeout are config, not payload.

### Live auth checks on the preview

| Request (signed out) | Result |
|---|---|
| `GET /api/beverages/sales` | **401** `{"data":null,"error":"You must be signed in."}`, `application/json` |
| `GET /beverages` | **307** → `/login?callbackUrl=…%2Fbeverages` |
| `GET /beverages/new-sale` | **307** → `/login?callbackUrl=…%2Fbeverages%2Fnew-sale` |

Both arms of the `authorized` callback still behave correctly in a production build.

---

## 5. State

- Commits on `main`: **`72fc56d`** (3.2 UI + the four verified fixes), **`889890a`** (CLAUDE.md
  convention note + Phase 3 marked done).
- Both pushed to `origin/main`.
- Nothing promoted to production.
- **Stopped** as instructed — delete-guard re-test and Phase 4 not started.

### Open, awaiting you

1. Whether to amend + force-push for the exact commit message (§1).
2. The delete-guard re-test — now needs sales re-created first, since the test DB was reset
   to baseline.
