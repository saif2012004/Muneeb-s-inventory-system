# Copy to `.env` and fill in. Never commit the filled-in `.env`.
#
# Use `.env`, not `.env.local`: the Prisma CLI only reads `.env`, and Next.js
# reads it as well, so one file serves both. Supabase Dashboard ->
# Project Settings -> Database -> Connection string.
#
# Percent-encode special characters in the password, or the URL will parse
# wrong / the dotenv parser will eat it:
#   &  ->  %26        $  ->  %24        @  ->  %40
#   #  ->  %23        ?  ->  %3F        /  ->  %2F

# POOLED - Supavisor TRANSACTION mode, port 6543. App runtime.
# Both query params are required (no prepared statements; one conn per invocation).
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-1-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# DIRECT - Supavisor SESSION mode, port 5432. `prisma migrate` only.
# Prefer the pooler host over db.<project-ref>.supabase.co, which is IPv6-only
# unless the paid IPv4 add-on is enabled.
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@aws-1-<region>.pooler.supabase.com:5432/postgres"

# Random 32+ char string. Generate: openssl rand -base64 32
NEXTAUTH_SECRET=""

# http://localhost:3000 in dev, the production URL on Vercel.
NEXTAUTH_URL="http://localhost:3000"
